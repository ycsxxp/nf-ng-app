import { RouterModule } from '@angular/router'

import { AuthorizeStatusComponent } from './authorize-status.component'

export const authorizeStatusLogRoutes = [
  {
    path: '',
    component: AuthorizeStatusComponent
  }
]