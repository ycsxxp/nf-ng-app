import { RouterModule } from '@angular/router'

import { AuthorizeComponent } from './authorize.component'

export const authorizeLogRoutes = [
  {
    path: '',
    component: AuthorizeComponent
  }
]